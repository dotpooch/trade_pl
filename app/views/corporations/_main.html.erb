<dl>

<% @corp.securities.each do |type| %>

<dt>

<%= type %>
</dt>


<dd>

<% if @corp.send(type).empty? %>
  None.

    <%#= type %>

<% else %>
</br>
<table id="sortable_list" class="table table-striped table-bordered table-condensed">
  
  <thead>
    <tr>
      <%= th("Equity","Symbol") %>
      <%= th("Equity","Start Date") %> 
      <%= th("Equity","End Date") %>
      <%= th("Equity","Count") %> 
      <%= th("Equity","Last Update") %> 
    </tr>
  </thead>
  
  <tbody>

  
  <% @corp.send(type).each do |i| %>
  
      <tr>
        <%= td(i.symbol) %>
        <%= i.start_date ? td_short_date(i.start_date) : td("-") %>
        <%= i.end_date ? td_short_date(i.end_date) : td("-") %>
        <%= i.count > 0 ? td_int(i.count) : td("-") %>
        <%= i.last_update ? td_short_date(i.last_update) : td("-") %>
      </tr>

    <% end %>
    
  </tbody>
  
<table>










  <%#= type %>




<% end %>
</br>
</br>

</dd>


<% end %>

</dl>
