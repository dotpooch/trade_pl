<table class="table table-striped table-bordered table-condensed">
<tbody>

  <% equity.symbols.reverse.each do |symbol| %>
  <tr>
    <%= td(link_to name, price_path(equity.symbols.last)) %>
  </tr>
  <% end %>

</tbody>
</table>
