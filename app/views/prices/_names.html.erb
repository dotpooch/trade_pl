<table class="table table-striped table-bordered table-condensed">
<tbody>

  <% names.reverse.each do |name| %>
  <tr>
    <td><%= td(link_to name, price_path(equity.symbols.last), class: "btn btn-info btn-mini" ) %></td>
  </tr>
  <% end %>

</tbody>
</table>
