<%= "The database has the following prices for #{@prices.first.names.last} (#{@prices.first.symbols.last}):" %><br>

<%#= link_to "All Price Data", prices_path %><br>
</br>

<%= render 'gchart_price', :symbol => @prices.first.symbols.last %>
<%= render 'shared_partials/prices' %>
